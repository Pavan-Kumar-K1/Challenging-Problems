class Solution:
    def lexicalOrder(self, n: int) -> List[int]:
        numbers = [str(i) for i in range(1,n+1)]
        sorted_numbers = sorted(numbers)
        numbers = [int(i) for i in sorted_numbers]
        return numbers
        
